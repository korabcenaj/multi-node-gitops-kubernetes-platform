apiVersion: v1
kind: Service
metadata:
  name: gitea-vps-egress
  namespace: flux-system
  annotations:
    tailscale.com/tailnet-fqdn: vps-proxy.tail42486f.ts.net
    tailscale.com/proxy-group: gitea-egress
spec:
  type: ExternalName
  externalName: placeholder
  ports:
    - name: https
      port: 443
      protocol: TCP
