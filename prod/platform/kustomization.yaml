apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # KEDA is intentionally disabled while no ScaledObjects or ScaledJobs exist.
  # Re-add keda.yaml before introducing a KEDA-managed workload.
  - metallb.yaml
  - oauth2-proxy.yaml
  - openldap.yaml
  - velero.yaml
  - velero-schedules.yaml
  - velero-offsite.yaml
  - velero-networkpolicy.yaml
  - velero-node-agent-config.yaml
