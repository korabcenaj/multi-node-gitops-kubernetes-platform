apiVersion: v1
kind: Namespace
metadata:
  name: backstage
  annotations:
    # Backstage developer portal scales to 0 overnight — it's a read-only dashboard.
    downscaler/uptime: "Mon-Sun 07:00-22:00 Europe/Berlin"
